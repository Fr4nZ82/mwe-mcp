name: mwe-truncate
version: 0.3.0
description: "Truncating context engine for the mwe-mcp sessionless model: bounded window of recent user turns + oversized tool-result snipping, no summarization pass — per-turn recall replaces the summary (the mwe-mcp hermes bridge, context half). Requires rotation mode (compression.in_place: false)."
