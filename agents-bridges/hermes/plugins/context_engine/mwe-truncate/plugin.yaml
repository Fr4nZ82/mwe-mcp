name: mwe-truncate
version: 0.2.0
description: "Truncating context engine for the mwe-mcp sessionless model: bounded window of recent user turns, no summarization pass — per-turn recall replaces the summary (the mwe-mcp hermes bridge, context half)."
