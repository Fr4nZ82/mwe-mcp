name: mwe
version: 0.3.0
description: "mwe-mcp persistent memory — one mechanical ingest+recall per turn over MCP, cross-consumer recent window injected verbatim (the mwe-mcp hermes bridge, memory half). Stdlib-only, no pip dependencies."
requires_env:
  - MWE_TOKEN
hooks:
  - on_memory_write
