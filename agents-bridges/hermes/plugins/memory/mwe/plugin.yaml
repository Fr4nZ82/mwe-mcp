name: mwe
version: 0.1.0
description: "mwe-mcp persistent memory — one mechanical ingest+recall per turn over MCP (the mwe-mcp hermes bridge, memory half). Stdlib-only, no pip dependencies."
requires_env:
  - MWE_TOKEN
hooks:
  - on_memory_write
