[toolchain]
# Pin to the latest stable channel for development; the MSRV (the actual
# floor) is declared in Cargo.toml `rust-version` — currently 1.88, per
# docs/development/conventions.md (MSRV).
channel = "stable"
components = ["rustfmt", "clippy", "rust-src"]
profile = "default"
